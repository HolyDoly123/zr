<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to t('chat'), '/users/sign_in', class: "navbar-brand"  %>
  <% if current_user %>
    <div class="dropdown ml-auto">
      <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <img class="avatar" src="<%= current_user.gravatar_url %>">
        <%= current_user.username %>
      </a>

      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <%= link_to t('logout'), destroy_user_session_path, method: :delete, class: 'dropdown-item' %>
      </div>
    </div>
  <% end %>
   <%= link_to 'eng', url_for(:locale => :en),class:'ml-auto'%>
  <span class="text-white">|</span>
  <%= link_to 'ru', url_for(:locale => :ru)%>

</nav>

